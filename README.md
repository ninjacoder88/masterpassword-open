# MasterPassword

## Environment Setup

Before running the security scan, create a `.env` file in the project root with the following variable:

```
APP_ROOT=src
```

`APP_ROOT` specifies the root directory of the application source code. It should be set to `src`.
