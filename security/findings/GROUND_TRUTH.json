{
  "summary": {
    "total_findings": 12,
    "critical": 0,
    "high": 5,
    "medium": 6,
    "low": 1
  },
  "findings": [
    {
      "id": "FINDING-001",
      "title": "Authentication tokens generated with predictable PRNG",
      "severity": "High",
      "owasp_category": "A02 - Cryptographic Failures",
      "cwe": "CWE-338",
      "file": "MasterPassword.BusinessLogic/TokenGenerator.cs",
      "line": 21,
      "description": "Authentication bearer tokens are generated with System.Random, which is not cryptographically secure. Tokens protect authenticated API access and should be generated with a CSPRNG.",
      "evidence": "Random random = new Random();\nstring str = \"\";\nfor (int i = 0; i < 64; i++)\n{\n    int asciiId = asciiIds[random.Next(0, asciiIds.Count)];\n    str += (char)asciiId;\n}",
      "attack_scenario": "An attacker who can observe token generation timing or obtain enough generated tokens can reduce the search space and predict future bearer tokens, then call protected endpoints as the victim while the predicted token is valid.",
      "remediation": "Use RandomNumberGenerator.GetBytes or RandomNumberGenerator.GetString with sufficient entropy for session tokens, and compare tokens using fixed-time comparison. Do not use System.Random for security tokens."
    },
    {
      "id": "FINDING-002",
      "title": "Generated passwords use predictable PRNG",
      "severity": "High",
      "owasp_category": "A02 - Cryptographic Failures",
      "cwe": "CWE-338",
      "file": "MasterPassword.BusinessLogic/RequestHandlers/GeneratePassword/GeneratePasswordRequestHandler.cs",
      "line": 16,
      "description": "The password generator uses System.Random to choose password characters. Passwords produced by a password manager must use cryptographically secure randomness.",
      "evidence": "Random random = new Random();\n...\nint r = random.Next(0, 4);\n...\nint v = random.Next(97, 123);",
      "attack_scenario": "A user may store or use a password generated by this endpoint. If an attacker can infer generation time or PRNG state, they can predict candidate passwords and attack downstream accounts protected by generated passwords.",
      "remediation": "Replace System.Random with System.Security.Cryptography.RandomNumberGenerator for all password character selection. Consider using RandomNumberGenerator.GetInt32 for unbiased index selection."
    },
    {
      "id": "FINDING-003",
      "title": "Vault encryption key is derived by base64 truncation instead of a KDF",
      "severity": "High",
      "owasp_category": "A02 - Cryptographic Failures",
      "cwe": "CWE-916",
      "file": "MasterPassword.BusinessLogic/KeyDeriver.cs",
      "line": 15,
      "description": "The encryption key for stored vault data is derived by base64-encoding the password, truncating or padding it to 32 characters, and using those bytes as the AES key. This provides no salt, memory hardness, or configurable work factor for offline resistance.",
      "evidence": "string encodedPassword = Convert.ToBase64String(Encoding.UTF8.GetBytes(password));\nreturn encodedPassword.Length > 32 ? encodedPassword.Substring(0, 32) : encodedPassword.PadLeft(32);",
      "attack_scenario": "If the database is leaked, an attacker can brute-force the user's master password directly against encrypted vault entries because the AES key derivation is cheap and deterministic.",
      "remediation": "Derive vault encryption keys with a password KDF such as Argon2id, scrypt, or PBKDF2 with a per-user random salt and strong work factor. Store the salt and KDF parameters with the account."
    },
    {
      "id": "FINDING-004",
      "title": "Encrypted vault data lacks authenticated encryption",
      "severity": "High",
      "owasp_category": "A02 - Cryptographic Failures",
      "cwe": "CWE-327",
      "file": "MasterPassword.BusinessLogic/Encryptor.cs",
      "line": 59,
      "description": "Sensitive account fields are encrypted with Aes.Create and a random IV, but no authentication tag or MAC is stored or verified. Ciphertext tampering cannot be reliably detected before decryption.",
      "evidence": "using (var aes = Aes.Create())\n{\n    aes.Key = key;\n    byte[] iv = aes.IV;\n    memoryStream.Write(iv, 0, iv.Length);\n    using (var cryptoStream = new CryptoStream(memoryStream, aes.CreateEncryptor(), CryptoStreamMode.Write))",
      "attack_scenario": "A database attacker can alter encrypted vault fields or tokens. The application will attempt to decrypt modified ciphertext, risking data corruption, chosen-ciphertext behavior, or service disruption because integrity is not checked.",
      "remediation": "Use an AEAD mode such as AES-GCM or ChaCha20-Poly1305 and store nonce plus authentication tag. If CBC must remain, add an HMAC over IV and ciphertext and verify it before decryption."
    },
    {
      "id": "FINDING-005",
      "title": "Master password policy permits four-character passwords",
      "severity": "Medium",
      "owasp_category": "A07 - Identification and Authentication Failures",
      "cwe": "CWE-521",
      "file": "MasterPassword.BusinessLogic/AppConfiguration.cs",
      "line": 6,
      "description": "The application allows primary account passwords as short as four characters. This is especially risky because the password is also used to derive the vault encryption key.",
      "evidence": "public const int PasswordMinimumLength = 4;",
      "attack_scenario": "An attacker can brute-force weak user passwords online or offline after database compromise, then decrypt the victim's saved credentials because the vault key is derived from the same weak password.",
      "remediation": "Require a substantially longer master password or passphrase, such as at least 12-14 characters, block known compromised passwords, and use rate limiting plus strong KDF parameters."
    },
    {
      "id": "FINDING-006",
      "title": "Session cookies are not configured with Secure or SameSite protections",
      "severity": "Medium",
      "owasp_category": "A05 - Security Misconfiguration",
      "cwe": "CWE-614",
      "file": "MasterPassword/Startup.cs",
      "line": 25,
      "description": "The session cookie is configured as HttpOnly and essential, but the code does not set Cookie.SecurePolicy or SameSite. Session cookies can be sent without explicit transport and cross-site constraints.",
      "evidence": "services.AddSession(options =>\n{\n    options.IdleTimeout = TimeSpan.FromMinutes(60);\n    options.Cookie.HttpOnly = true;\n    options.Cookie.IsEssential = true;\n});",
      "attack_scenario": "If the app is reachable over HTTP or behind a misconfigured proxy, the browser may send the session cookie over an insecure channel. Missing SameSite also weakens protection against cross-site requests that rely on ambient session cookies.",
      "remediation": "Set options.Cookie.SecurePolicy = CookieSecurePolicy.Always and options.Cookie.SameSite = SameSiteMode.Strict or Lax based on the product flow. Ensure production terminates only HTTPS traffic."
    },
    {
      "id": "FINDING-007",
      "title": "Application pipeline does not enforce HTTPS or HSTS",
      "severity": "Medium",
      "owasp_category": "A05 - Security Misconfiguration",
      "cwe": "CWE-319",
      "file": "MasterPassword/Startup.cs",
      "line": 67,
      "description": "The ASP.NET request pipeline serves static files, routing, sessions, authentication, and authorization without UseHttpsRedirection or UseHsts. Sensitive login, session, and vault traffic is not enforced as HTTPS by the application.",
      "evidence": "app.UseStaticFiles()\n    .UseRouting()\n    .UseSession();\n\napp.UseAuthentication()\n    .UseAuthorization()\n    .UseHealthChecks(\"/health\");",
      "attack_scenario": "A deployment or reverse-proxy mistake can expose the application over plain HTTP, allowing network attackers to capture credentials, bearer tokens, session cookies, and decrypted vault data in transit.",
      "remediation": "Add UseHttpsRedirection and production UseHsts, configure forwarded headers correctly behind proxies, and reject insecure requests at the edge."
    },
    {
      "id": "FINDING-008",
      "title": "Logout is a state-changing unauthenticated GET endpoint",
      "severity": "Medium",
      "owasp_category": "A01 - Broken Access Control",
      "cwe": "CWE-352",
      "file": "MasterPassword/Controllers/API/v1/PrimaryAccountsController.cs",
      "line": 49,
      "description": "The logout action changes server-side token state and session state, but it is exposed as GET and has no Authorize attribute. This makes it vulnerable to cross-site request triggering and inconsistent authorization behavior.",
      "evidence": "[HttpGet(\"Logout\")]\npublic async Task<IActionResult> Logout() =>\n    await ActionHandler.HandleAsync(async () =>\n    {\n        LogoutResponse response = await LogoutRequestHandler.HandleAsync(new LogoutRequest() { Id = HttpContext.GetUserId() });\n        HttpContext.Logout();\n        return response;\n    });",
      "attack_scenario": "An attacker can embed an image or link to /api/PrimaryAccounts/Logout. When a logged-in victim's browser requests it, the application can clear the victim's session and token without user intent.",
      "remediation": "Require authentication for logout, change it to POST, and enforce CSRF protections or an anti-forgery token for cookie/session-backed flows."
    },
    {
      "id": "FINDING-009",
      "title": "Users can create notes on another user's secondary account",
      "severity": "High",
      "owasp_category": "A01 - Broken Access Control",
      "cwe": "CWE-639",
      "file": "MasterPassword.BusinessLogic/RequestHandlers/CreateNote/CreateNoteRequestHandler.cs",
      "line": 24,
      "description": "CreateNote loads the requested SecondaryAccountId and creates a note for it, but does not verify that the secondary account belongs to the authenticated PrimaryAccountId. The source comment also acknowledges the missing ownership validation.",
      "evidence": "//todo: add more validation relating to the secondary account belonging to the primary account\n...\nvar secondaryAccount = await Repository.LoadSecondaryAccountByIdAsync(request.SecondaryAccountId);\n...\nvar note = new Note { Title = encryptedTitle, Description = encryptedDescription, SecondaryAccountId = secondaryAccount._id.ToString() };",
      "attack_scenario": "An authenticated attacker who obtains or guesses a victim secondary account id can create notes attached to the victim's account. The victim may later load corrupted or attacker-controlled encrypted note data, causing integrity loss or denial of service for that account item.",
      "remediation": "Before creating the note, load the secondary account and require secondaryAccount.PrimaryAccountId == request.PrimaryAccountId. Return a generic authorization failure when ownership does not match."
    },
    {
      "id": "FINDING-010",
      "title": "Public favicon refresh endpoint can trigger server-side requests",
      "severity": "Medium",
      "owasp_category": "A10 - Server-Side Request Forgery",
      "cwe": "CWE-918",
      "file": "MasterPassword/Controllers/API/v1/FaviconsController.cs",
      "line": 13,
      "description": "The favicon refresh endpoint has no Authorize attribute and calls a handler that sends outbound HTTP requests to hosts and URLs stored from user-controlled account URLs. This creates a public trigger for server-side requests.",
      "evidence": "[HttpGet]\npublic async Task<IActionResult> Get() => await ActionHandler.HandleAsync(async () =>\n    await UpdateFaviconRequestHandler.HandleAsync(new UpdateFaviconRequest()));",
      "attack_scenario": "An attacker can register, store a secondary account URL pointing at an internal host, and then call the unauthenticated favicon endpoint to make the server request internal HTTPS resources such as https://localhost/favicon.ico or cloud metadata-style hostnames if reachable.",
      "remediation": "Require authorization for favicon refresh, move refresh to a controlled background job, validate and allowlist outbound hosts, reject private/link-local/loopback addresses, and set strict timeout and response-size limits."
    },
    {
      "id": "FINDING-011",
      "title": "Raw exception messages are returned to clients",
      "severity": "Low",
      "owasp_category": "A05 - Security Misconfiguration",
      "cwe": "CWE-209",
      "file": "MasterPassword/Controllers/API/v1/FaviconsController.cs",
      "line": 26,
      "description": "The shared ActionHandler returns ExceptionResponse(ex.Message) directly to API clients. Internal parse, database, crypto, and network errors can leak implementation details useful for further attacks.",
      "evidence": "catch(Exception ex)\n{\n    return new JsonResult(new ExceptionResponse(ex.Message));\n}",
      "attack_scenario": "An attacker can submit malformed ids, ciphertext-dependent inputs, or invalid URLs and use returned exception text to learn internal object id formats, dependency behavior, stack-sensitive error conditions, or network access details.",
      "remediation": "Return a generic error message and appropriate status code to clients. Log detailed exception information server-side with request correlation ids."
    },
    {
      "id": "FINDING-012",
      "title": "Account lockout can be abused for denial of service",
      "severity": "Medium",
      "owasp_category": "A07 - Identification and Authentication Failures",
      "cwe": "CWE-307",
      "file": "MasterPassword.BusinessLogic/RequestHandlers/Login/LoginRequestHandler.cs",
      "line": 38,
      "description": "Failed login attempts permanently lock the account once FailedLoginCount exceeds five, and the check happens before password verification. There is no time window, unlock workflow, IP throttling, or reset path for a legitimate password after lockout.",
      "evidence": "if (primaryAccountByEmail.FailedLoginCount > 5)\n    return new LoginResponse(false) { ErrorMessage = \"Login failed. Account has been locked out due to too many failed logins.\" };\n\nif (!Encryptor.PlainTextMatchesHash(request.Password, primaryAccountByEmail.Password))\n{\n    await Repository.UpdateFailedLoginCountAsync(primaryAccountByEmail._id, primaryAccountByEmail.FailedLoginCount + 1);",
      "attack_scenario": "An attacker who knows or enumerates a victim email address can submit six wrong passwords and prevent the victim from logging in even with the correct password, causing account-level denial of service.",
      "remediation": "Use rate limiting by account, IP, and device; apply temporary exponential backoff instead of permanent lockout; provide a secure unlock or recovery flow; and avoid revealing lockout state unnecessarily."
    }
  ]
}
